console.log(`wtf wth wth nbsg`);

// 

const calage = function (byear) {
    return 2024 - byear;
}

// arrow function

const realage = agger=> 2024 - agger;
const age_new = calage(2004);
// console.log(age_new);


const years_until_job = birth => {
    const age = 2024 - birth;
    if (age >= 60) {
        console.log(`Time for quite job because age is older than ${age} `);
    } else {
        console.log(`time left for quite job is ${60 - age} Year after you got quite `);
        
    }
    
    return years_until_job;
    
};

years_until_job(1966);